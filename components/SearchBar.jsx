import React from 'react';
import {View} from "react-native";

const SearchBar = () => {
    return (
        <View style={{
            flex: 1,
            maxHeight: 60,
        }}>

        </View>
    );
};

export default SearchBar;